<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="NET_MAUI_BLE.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:NET_MAUI_BLE"
    xmlns:pages="clr-namespace:NET_MAUI_BLE.Pages"
    Shell.FlyoutBehavior="Disabled">

    <TabBar>
    <!--  
    TODO - Comment out once Design aspect is complete
        <Tab Title="Home">
            <ShellContent ContentTemplate="{DataTemplate pages:HomePage}" />
        </Tab>
    -->
        <Tab Title="Home">
            <ShellContent ContentTemplate="{DataTemplate pages:SamplePage}" />
        </Tab>
        <Tab Title="Setting">
            <ShellContent ContentTemplate="{DataTemplate pages:SettingPage}" />
        </Tab>
        <Tab Title="Audio">
            <ShellContent ContentTemplate="{DataTemplate pages:AudioPage}" />
        </Tab>
        <Tab Title="GraphPage">
            <ShellContent ContentTemplate="{DataTemplate pages:GraphPage}" />
        </Tab>
        <Tab Title="FilePage">
            <ShellContent ContentTemplate="{DataTemplate pages:FilePage}" />
        </Tab>
    </TabBar>

</Shell>
