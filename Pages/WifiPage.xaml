<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="NET_MAUI_BLE.Pages.WifiPage">
    <StackLayout Margin="20"
                 Spacing="6">
        <!--<Button Text="Get audio file"
                WidthRequest="200"
                Command="{Binding GetAudioCommand}"/>
        <Button Text="Get streaming audio"
                WidthRequest="200"
                Command="{Binding GetStreamAudioCommand}"/>
        <Button Text="Test"
                WidthRequest="200"
                Command="{Binding TestCommand}"/>-->
        <Button Text="Send L"
                WidthRequest="200"
                Command="{Binding SendLCommand}"/>
        <Button Text="Send H"
                WidthRequest="200"
                Command="{Binding SendHCommand}"/>
        <!--<Label Text="{Binding TestLabel}"
               HorizontalTextAlignment="Center"
               FontSize="Large"/>-->
    </StackLayout>
</ContentPage>
